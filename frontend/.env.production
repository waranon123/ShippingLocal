# Production Environment for Cloudflare Workers Backend
VITE_API_BASE_URL=https://truck-management-backend-production.robotx99.workers.dev
VITE_ENVIRONMENT=production

# Note: WebSocket might not work directly with Workers
# Consider using Server-Sent Events or polling instead